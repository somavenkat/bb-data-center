# Center Viewer

This is a minimal Vite + React app that reads all JSON files from the project root and displays selected fields.

Run locally:

```bash
npm install
npm run dev
```

Open the shown URL (usually http://localhost:5173) to view centers.
